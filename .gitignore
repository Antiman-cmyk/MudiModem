build/
.superpowers/
.worktrees/

# Vendor manual — copyrighted Quectel PDF, kept locally for reference only.
# Its verified contents live in reference/quectel-at-reference.md.
docs/*.pdf

# Python bytecode
__pycache__/
*.pyc

# GL firmware images kept locally for analysis — never commit (hundreds of MB).
# Drop them under firmware/ (see firmware/README.md); only that README is tracked.
firmware/*
!firmware/README.md
*.img
